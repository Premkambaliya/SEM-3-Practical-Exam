insert into Hotel values
(1,"abc","abc city",10),
(2,"xyz","xyz city",20)

UPDATE Hotel 
SET rooms_available = 15
WHERE hotel_id=1;

DELETE From Hotel
WHERE hotel_id = 2;

SELECT 
    SUM(rooms_available) AS total_rooms,
    AVG(rooms_available) AS average_rooms
FROM Hotel;
