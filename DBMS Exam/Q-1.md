SHOW DATABASES;

CREATE DATABASE University;

use University;
create table Hotel(
hotel_id int primary key,
hotel_name varchar(60) unique,
city varchar(40),
rooms_available int 
);

select * from Hotel;